local cfg = {}

-- ONLY EDIT LINES BETWEEN THE HASHES
-- ##################################

cfg.cad_url = "https://demo.cadvanced.app" -- The full URL of your CAD
cfg.api_token = "123" -- Your CAD's API token, see Admin > Preferences in the CAD
cfg.enable_whitelist = true -- Only allow players with the "Player" role to join
cfg.sound_volume = 0.5 -- The volume of MDT sounds

-- ##################################

return cfg
